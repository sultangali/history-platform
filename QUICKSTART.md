# Быстрый старт 🚀

## Минимальная установка (5 минут)

### 1. Установите зависимости

**Backend:**
```bash
cd server
npm install
```

**Frontend:**
```bash
cd client
npm install
```

### 2. Настройте окружение

Создайте файл `server/.env`:
```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/repression-archive
JWT_SECRET=my_secret_key_123
NODE_ENV=development
```

### 3. Убедитесь что MongoDB запущен

```bash
# Linux/Mac
mongosh

# Должно подключиться без ошибок
```

### 4. Заполните тестовыми данными (опционально)

```bash
cd server
npm run seed
```

Это создаст:
- 3 пользователя (admin, moderator, user)
- 6 примеров дел о репрессиях

**Учетные данные:**
```
Админ:      admin@karagandymemory.kz / KRG_MEMadmin1122
Модератор:  moderator@karagandymemory.kz / KRG_MEMmoderator332211
Пользователь: user@karagandymemory.kz / KRG_MEMuser741852963
```

### 5. Запустите проект

**Терминал 1 (Backend):**
```bash
cd server
npm run dev
```

**Терминал 2 (Frontend):**
```bash
cd client
npm run dev
```

### 6. Откройте в браузере

Перейдите на http://localhost:3000

## Тестирование

### Как пользователь (без входа):
1. Откройте главную страницу
2. Перейдите в "Архив" (Мұрағат)
3. Попробуйте поиск и фильтры
4. Откройте любое дело
5. Предложите исправление

### Как модератор:
1. Войдите: moderator@archive.kz / moderator123
2. В меню выберите "Профиль" → "Модератор панелі"
3. Нажмите "Жаңа іс қосу"
4. Заполните форму и сохраните

### Как администратор:
1. Войдите: admin@archive.kz / admin123
2. В меню выберите "Профиль" → "Әкімші панелі"
3. Управляйте пользователями

## Языки

Переключение в правом верхнем углу:
- 🇰🇿 Қазақша (по умолчанию)
- 🇷🇺 Русский
- 🇬🇧 English

## Структура URL

```
/                  - Главная страница
/archive           - Каталог дел
/cases/:id         - Детали дела
/login             - Вход
/register          - Регистрация
/contact           - Обратная связь
/about             - О проекте
/moderator         - Панель модератора (требует роль)
/admin             - Панель администратора (требует роль)
```

## API Endpoints

```
GET    /api/health                - Проверка работы сервера
POST   /api/auth/register         - Регистрация
POST   /api/auth/login            - Вход
GET    /api/auth/me               - Текущий пользователь

GET    /api/cases                 - Список дел
GET    /api/cases/:id             - Детали дела
POST   /api/cases                 - Создать дело (модератор)
PUT    /api/cases/:id             - Обновить дело (модератор)
DELETE /api/cases/:id             - Удалить дело (модератор)

POST   /api/suggestions           - Предложить правку
GET    /api/suggestions           - Список предложений (модератор)
PUT    /api/suggestions/:id       - Обновить статус (модератор)

POST   /api/feedback              - Отправить обратную связь
GET    /api/feedback              - Список обратной связи (модератор)

GET    /api/users                 - Список пользователей (админ)
PUT    /api/users/:id             - Обновить пользователя (админ)
```

## Возможные проблемы

### MongoDB не запущен
```bash
sudo systemctl start mongod
```

### Порт занят
Измените PORT в `server/.env` на другой (например, 5001)

### Нет данных в архиве
Запустите `npm run seed` в папке server

---

Для полной инструкции смотрите SETUP.md


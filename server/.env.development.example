# ===========================================
# DEVELOPMENT CONFIGURATION
# Используется для локальной разработки
# ===========================================

# Режим проекта (переключатель dev/prod)
PROJECT_MODE=dev

# Порт сервера
PORT=5000

# Окружение Node.js
NODE_ENV=development

# MongoDB подключение (локальная база для dev)
# Используйте отдельную базу для разработки, чтобы не затронуть production данные
MONGODB_URI=mongodb://localhost:27017/repression-archive-dev

# JWT секретный ключ (для разработки - простой)
# В production используйте сложный автогенерированный ключ!
JWT_SECRET=dev_secret_key_change_in_production

# Срок действия JWT токена
JWT_EXPIRES_IN=30d

# CORS настройки (для dev разрешаем все источники)
CORS_ORIGIN=*

# Опционально: настройки для логирования
DEBUG=true

# Қарағанды облысы - Саяси қуғын-сүргін мұрағаты
# Архив политических репрессий Карагандинской области

> Платформа для документации и исследования политических репрессий на территории Карагандинской области

## 🚀 Быстрый старт

### Предварительные требования
- Node.js 18+
- MongoDB (локально или MongoDB Atlas)
- npm

### Установка

**1. Клонируйте репозиторий:**
```bash
git clone git@github.com:sultangali/history-platform.git
cd history-platform
```

**2. Установите зависимости:**
```bash
# Backend
cd server
npm install

# Frontend
cd ../client
npm install
```

**3. Настройте окружение:**

Используйте удобный скрипт переключения режимов:

```bash
# Автоматически скопирует .env файлы для development режима
./switch-mode.sh dev
```

Или вручную создайте файлы конфигурации:

```bash
# Backend
cd server
cp .env.development.example .env

# Frontend
cd ../client
cp .env.development.example .env
```

**4. Запустите MongoDB:**
```bash
# Linux/Mac
sudo systemctl start mongod

# или используйте MongoDB Atlas
```

**5. Заполните тестовыми данными (опционально):**
```bash
cd server
npm run seed
```

Будут созданы тестовые пользователи:
- **Админ:** admin@karagandymemory.kz / KRG_MEMadmin1122
- **Модератор:** moderator@karagandymemory.kz / KRG_MEMmoderator332211
- **Пользователь:** user@karagandymemory.kz / KRG_MEMuser741852963

**6. Запустите проект:**

Откройте два терминала:

```bash
# Терминал 1 - Backend
cd server
npm run dev

# Терминал 2 - Frontend
cd client
npm run dev
```

**7. Откройте браузер:**
- Frontend: http://localhost:3000
- Backend API: http://localhost:5000

## 🔄 Переключение между Dev и Production

### Быстрый способ (рекомендуется)

Используйте встроенный скрипт:

```bash
# Переключить в development режим
./switch-mode.sh dev

# Переключить в production режим
./switch-mode.sh prod
```

Скрипт автоматически копирует соответствующие `.env` файлы в `server/` и `client/`.

### Ручной способ

Проект поддерживает два режима работы через переменную `PROJECT_MODE`:

### Development (локальная разработка)
```bash
# server/.env
PROJECT_MODE=dev
PORT=5000
MONGODB_URI=mongodb://localhost:27017/repression-archive-dev
JWT_SECRET=dev_secret_key_change_in_production
NODE_ENV=development

# client/.env
PROJECT_MODE=dev
VITE_API_URL=http://localhost:5000/api
```

### Production (боевой сервер)
```bash
# server/.env
PROJECT_MODE=prod
PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/repression-archive
JWT_SECRET=<auto-generated-secure-key>
NODE_ENV=production
CORS_ORIGIN=https://yourdomain.com

# client/.env.production
PROJECT_MODE=prod
VITE_API_URL=https://yourdomain.com/api
```

## 📁 Структура проекта

```
history-platform/
├── client/                 # Frontend (React + Vite)
│   ├── src/
│   │   ├── components/     # Компоненты
│   │   ├── pages/          # Страницы
│   │   ├── contexts/       # React Context
│   │   ├── services/       # API клиент
│   │   ├── i18n/           # Переводы (kk, ru, en)
│   │   └── styles/         # CSS стили
│   ├── .env.development.example
│   └── .env.production.example
│
├── server/                 # Backend (Node.js + Express)
│   ├── models/             # MongoDB модели
│   ├── routes/             # API роуты
│   ├── middleware/         # Middleware (auth)
│   ├── utils/              # Утилиты (seed скрипты)
│   ├── .env.development.example
│   └── .env.production.example
│
├── deploy/                 # Deployment скрипты
├── PROJECT_STRUCTURE.md    # Архитектура проекта
└── README.md               # Этот файл
```

Подробная архитектура описана в [PROJECT_STRUCTURE.md](./PROJECT_STRUCTURE.md)

## ✨ Возможности

- ✅ **Публичный архив** - доступ без регистрации
- ✅ **Поиск и фильтрация** - по ФИО, месту, году
- ✅ **Трехязычный интерфейс** - Қазақша (по умолчанию), Русский, English
- ✅ **Система ролей** - Пользователь, Модератор, Администратор
- ✅ **Предложения правок** - пользователи могут сообщать об ошибках
- ✅ **Панель модератора** - управление делами
- ✅ **Панель администратора** - управление пользователями
- ✅ **Адаптивный дизайн** - работает на всех устройствах

## 🛠 Технологии

**Frontend:**
- React 18 + Vite
- React Router v6
- i18next (трехязычность)
- Framer Motion (анимации)
- Axios

**Backend:**
- Node.js + Express
- MongoDB + Mongoose
- JWT аутентификация
- bcryptjs

## 📚 API Документация

```
POST   /api/auth/register        - Регистрация
POST   /api/auth/login           - Вход
GET    /api/auth/me              - Текущий пользователь

GET    /api/cases                - Список дел (с фильтрами)
GET    /api/cases/:id            - Детали дела
POST   /api/cases                - Создать дело (модератор)
PUT    /api/cases/:id            - Обновить дело (модератор)
DELETE /api/cases/:id            - Удалить дело (модератор)

GET    /api/suggestions          - Список предложений (модератор)
POST   /api/suggestions          - Создать предложение
PUT    /api/suggestions/:id      - Обновить статус (модератор)

GET    /api/feedback             - Список обратной связи (модератор)
POST   /api/feedback             - Отправить обратную связь

GET    /api/users                - Список пользователей (админ)
PUT    /api/users/:id            - Обновить пользователя (админ)
```

## 👥 Роли пользователей

### Пользователь (user)
- Просмотр архива дел
- Поиск и фильтрация
- Отправка предложений по исправлению
- Отправка обратной связи

### Модератор (moderator)
- Все права пользователя +
- Добавление и редактирование дел
- Обработка предложений
- Просмотр обратной связи

### Администратор (admin)
- Все права модератора +
- Управление пользователями
- Назначение ролей
- Блокировка пользователей

## 🌐 Языки

Платформа поддерживает три языка:
- **🇰🇿 Қазақша** (по умолчанию)
- **🇷🇺 Русский**
- **🇬🇧 English**

Переключение языка доступно в шапке сайта.

## 🚢 Деплой в Production

Проект готов к деплою на сервер. Используйте скрипты из папки `deploy/`:

1. **Установка зависимостей:** `01-install-dependencies.sh`
2. **Деплой приложения:** `02-deploy-app.sh`
3. **Настройка Nginx:** `03-setup-nginx.sh`
4. **Настройка SSL:** `04-setup-ssl.sh`
5. **Полный деплой:** `05-full-deploy.sh`

Подробнее в [deploy/DEPLOYMENT_NOTES.md](./deploy/DEPLOYMENT_NOTES.md)

## 📝 Скрипты

**Backend:**
```bash
npm start        # Запуск в production
npm run dev      # Запуск в development (hot reload)
npm run seed     # Заполнить БД тестовыми данными
npm run clean    # Очистить тестовые данные
```

**Frontend:**
```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для production
npm run preview  # Превью production сборки
```

## 🔐 Безопасность

- ✅ JWT токены (30 дней)
- ✅ Bcrypt хеширование паролей (10 rounds)
- ✅ Защищенные маршруты (middleware)
- ✅ Проверка ролей на уровне API
- ✅ CORS настройки

## 🤝 Участие в разработке

1. Fork проекта
2. Создайте ветку (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Проект создан в образовательных и исторических целях.

## 📧 Контакты

Для вопросов и предложений: archive@karaganda.kz

---

**Версия:** 1.0.0  
**Статус:** ✅ В продакшене  
© 2025 Қарағанды облысы мұрағаты. Барлық құқықтар қорғалған.

# Инструкция по установке и запуску платформы

## 📋 Предварительные требования

Перед началом убедитесь, что у вас установлено:

- **Node.js** версии 18.0 или выше ([скачать](https://nodejs.org/))
- **MongoDB** (локально или удаленно через MongoDB Atlas)
- **npm** (устанавливается вместе с Node.js)

## 🔧 Установка

### Шаг 1: Клонирование или создание проекта

Если вы скачали проект, перейдите в директорию:
```bash
cd /home/sultangali/Documents/history
```

### Шаг 2: Установка зависимостей сервера

```bash
cd server
npm install
```

Установятся следующие пакеты:
- express
- mongoose
- cors
- bcryptjs
- jsonwebtoken
- dotenv
- multer

### Шаг 3: Настройка базы данных и переменных окружения

Создайте файл `.env` в папке `server`:

```bash
# В папке server создайте файл .env
nano .env
```

Добавьте следующие переменные:

```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/repression-archive
JWT_SECRET=your_very_secure_secret_key_change_this_in_production
NODE_ENV=development
```

**Важно:** 
- Если используете MongoDB Atlas, замените `MONGODB_URI` на вашу строку подключения
- Обязательно смените `JWT_SECRET` на уникальное значение для продакшена

### Шаг 4: Установка и запуск MongoDB (если локально)

**Ubuntu/Linux:**
```bash
sudo systemctl start mongod
sudo systemctl enable mongod
```

**macOS (через Homebrew):**
```bash
brew services start mongodb-community
```

**Windows:**
Запустите MongoDB через Services или командную строку

### Шаг 5: Установка зависимостей клиента

```bash
cd ../client
npm install
```

Установятся следующие пакеты:
- react
- react-dom
- react-router-dom
- i18next и react-i18next
- axios
- framer-motion
- react-bootstrap-icons

## 🚀 Запуск проекта

### Запуск в режиме разработки

Вам нужно запустить два процесса параллельно:

**Терминал 1 - Backend:**
```bash
cd server
npm run dev
```

Сервер запустится на http://localhost:5000

**Терминал 2 - Frontend:**
```bash
cd client
npm run dev
```

Приложение откроется на http://localhost:3000

### Проверка работы сервера

Откройте в браузере: http://localhost:5000/api/health

Должно вернуться:
```json
{
  "status": "OK",
  "message": "Server is running"
}
```

## 👤 Создание первого пользователя

### 1. Регистрация обычного пользователя

Откройте http://localhost:3000/register и зарегистрируйтесь

### 2. Создание администратора

После регистрации подключитесь к MongoDB и измените роль:

**Через MongoDB Shell:**
```bash
mongosh
```

```javascript
use repression-archive

// Найдите вашего пользователя
db.users.find({ email: "your-email@example.com" })

// Сделайте его администратором
db.users.updateOne(
  { email: "your-email@example.com" },
  { $set: { role: "admin" } }
)
```

**Или через MongoDB Compass:**
1. Подключитесь к базе данных
2. Откройте коллекцию `users`
3. Найдите вашего пользователя
4. Измените поле `role` с `user` на `admin`

### 3. Создание модератора

Аналогично, но установите `role: "moderator"`:

```javascript
db.users.updateOne(
  { email: "moderator@example.com" },
  { $set: { role: "moderator" } }
)
```

## 📝 Структура ролей

### User (Пользователь)
- Просмотр архива
- Поиск и фильтрация
- Отправка предложений
- Отправка обратной связи

### Moderator (Модератор)
- Все права пользователя +
- Добавление дел
- Редактирование дел
- Обработка предложений
- Просмотр обратной связи

### Admin (Администратор)
- Все права модератора +
- Управление пользователями
- Назначение ролей
- Блокировка/разблокировка пользователей

## 🔍 Тестирование функционала

### Тест публичного доступа
1. Откройте http://localhost:3000 (без авторизации)
2. Перейдите в Архив
3. Попробуйте поиск и фильтры
4. Откройте детали дела
5. Отправьте предложение по исправлению

### Тест модератора
1. Войдите как модератор
2. Перейдите на `/moderator`
3. Добавьте новое дело
4. Просмотрите предложения

### Тест администратора
1. Войдите как администратор
2. Перейдите на `/admin`
3. Просмотрите список пользователей
4. Измените роль пользователя

## 🌐 Переключение языка

Язык можно переключить в правом верхнем углу:
- 🇰🇿 Қазақша (по умолчанию)
- 🇷🇺 Русский
- 🇬🇧 English

## 📦 Сборка для продакшена

### Backend
```bash
cd server
npm start
```

### Frontend
```bash
cd client
npm run build
```

Соберется в папку `client/dist`

### Деплой

Можно использовать:
- **Backend**: Heroku, Railway, DigitalOcean
- **Frontend**: Vercel, Netlify, GitHub Pages
- **База данных**: MongoDB Atlas

## ❗ Возможные проблемы

### Ошибка подключения к MongoDB
```
MongoNetworkError: failed to connect to server
```
**Решение:** Убедитесь что MongoDB запущен и доступен

### Ошибка CORS
```
Access-Control-Allow-Origin error
```
**Решение:** Проверьте настройки CORS в `server/server.js`

### Ошибка JWT
```
jwt malformed
```
**Решение:** Проверьте наличие `JWT_SECRET` в `.env`

### Порты заняты
```
Port 5000 is already in use
```
**Решение:** Измените PORT в `.env` или остановите процесс на порту

## 📧 Поддержка

При возникновении проблем проверьте:
1. Версию Node.js: `node --version` (должна быть >= 18)
2. Статус MongoDB: `mongosh` должен подключиться
3. Файл `.env` создан и заполнен корректно
4. Все зависимости установлены: `npm install`

## ✅ Чек-лист запуска

- [ ] Node.js установлен
- [ ] MongoDB установлен и запущен
- [ ] Зависимости сервера установлены
- [ ] Файл `.env` создан
- [ ] Зависимости клиента установлены
- [ ] Сервер запущен на порту 5000
- [ ] Клиент запущен на порту 3000
- [ ] Первый администратор создан
- [ ] Интерфейс открывается в браузере

Успешной работы! 🎉

